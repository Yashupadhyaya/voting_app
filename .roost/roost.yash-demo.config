app_dependencies:
  type: text
  content: |-
    #!/bin/sh

    # make all
    # docker build
app_deploy:
  type: text
  content: |+
    #/bin/sh

    HELM_OPTS="--set clusterIP=${ROOST_CLUSTER_IP},namespace=${ROOST_NAMESPACE},registry=zbio"
    helm upgrade pulumi-eks-helm-vote helm-vote -n $ROOST_NAMESPACE --create-namespace --install $HELM_OPTS
    kubectl expose deployment voter --type=LoadBalancer --name=my-service -n $ROOST_NAMESPACE

app_test: []
app_uninstall:
  type: text
  content: |+
    #!/bin/sh

build:
  type: text
  content: |+
    #!/bin/sh

deploy:
  type: text
  content: |-
    #/bin/sh
    HELM_OPTS="--set clusterIP=${ROOST_CLUSTER_IP},namespace=${ROOST_NAMESPACE},registry=zbio",tag=latest
    helm upgrade yash-helm-vote helm-vote -n $ROOST_NAMESPACE --create-namespace --install $HELM_OPTS
test: []
uninstall:
  type: text
  content: |+
    #!/bin/sh

dockerCompose: ''
app_docker_compose: ''
lastUpdatedBy: Yash Upadhyay
lastUpdatedOn: 2023-03-06T12:56:42.524Z
